<!DOCTYPE html>
<html lang="en">
  <link rel="shortcut icon" type="image/x-icon" href="/Images/relove.png" />
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>LOVIZ LOGIN</title>
    <!-- Link to your website's theme CSS (example: TailwindCSS) -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet" />
    <style>
      body {
        font-family: 'Arial', sans-serif; /* Set your website's font */
        background-color: #4e7f5e; /* Set the background color matching the website */
        color: #333; /* Default text color */
      }
      .login-container {
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
        padding: 1rem;
      }
      .login-form {
        background-color: white;
        padding: 2rem;
        border-radius: 1rem;
        box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
        width: 100%;
        max-width: 450px;
      }
      h2 {
        font-size: 2rem;
        font-weight: 700;
        text-align: center;
        margin-bottom: 1.5rem;
        color: #1a202c;
      }
      .form-group {
        margin-bottom: 1.25rem;
      }
      .form-control {
        padding: 0.75rem;
        border-radius: 0.5rem;
        border: 1px solid #ddd;
        width: 100%;
      }
      .btn {
        padding: 0.75rem;
        width: 100%;
        border-radius: 0.5rem;
        font-size: 1.125rem;
      }
      .btn-dark {
        background-color: #1a202c;
        color: white;
        border: none;
      }
      .btn-outline-secondary {
        border-color: #e2e8f0;
        color: #333;
      }
      .signup-link {
        text-align: center;
        margin-top: 1rem;
      }
    </style>
     <link href="https://fonts.googleapis.com/css2?family=Amatic+SC:wght@400;700&family=Gruppo&display=swap" rel="stylesheet">
    
     <style>
       .amatic-sc-regular {
         font-family: "Amatic SC", serif;
         font-weight: 400;
       }
       .amatic-sc-bold {
         font-family: "Amatic SC", serif;
         font-weight: 700;
       }
       .gruppo-regular {
         font-family: "Gruppo", serif;
         font-weight: 400;
         font-style: normal;
       }
 
     </style>
  </head>
  <body>
    <div class="login-container">
      <form class="login-form" action="/login" method="post">
        <h2 class="gruppo-regular " style="font-size: 2.4rem;"><span style="color: #4e7f5e;">L</span>OGIN</h2>
        <div class="form-group">
          <label class="gruppo-regular " style="font-size: 1.5rem;" for="email">Email</label>
          <input
            type="email"
            id="email"
            name="email"
            placeholder="Enter your email"
            class="form-control"
            required
          />
        </div>
        <button type="submit" class="btn btn-dark">Login</button>
        <div class="signup-link">
          <span class="gruppo-regular " style="font-size: 1.2rem; ">Don't have an account?</span>
          <br>
          <br>
          <a href="/register" class="btn btn-outline-secondary gruppo-regular" style=" font-size: 1rem; background-color: #4e7f5e; border-radius: 8px; color: white;">Create new account</a>
        </div>
      </form>
    </div>

    <!-- Optional JS scripts (Bootstrap, jQuery) -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
  </body>
</html>
